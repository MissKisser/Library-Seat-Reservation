version: "3.8"
services:
  seatbot:
    build: .
    image: seatbot:latest
    container_name: seatbot
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Shanghai
